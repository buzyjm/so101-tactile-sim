import { strictEqual } from 'node:assert';

// Starter test so `npm test` passes on a fresh project.
// Replace this with your own tests, or add more files under src/tests/.
describe('smoke test', () => {
    it('runs the test suite', () => {
        strictEqual(1 + 1, 2);
    });
});
